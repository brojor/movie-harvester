name: Build & Push TMDB Worker

on:
  push:
    branches: [main, docker]
    paths:
      - 'apps/workers/tmdb/**'
      - Dockerfile

permissions:
  contents: read
  packages: write

jobs:
  build-and-push-tmdb-worker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push tmdb-worker
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: tmdb-worker
          push: true
          tags: ghcr.io/${{ github.repository }}-tmdb-worker:latest
