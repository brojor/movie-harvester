name: Build & Push Webshare Worker

on:
  push:
    branches: [main, docker]
    paths:
      - 'apps/workers/webshare/**'
      - Dockerfile

permissions:
  contents: read
  packages: write

jobs:
  build-and-push-webshare-worker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push webshare-worker
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: webshare-worker
          push: true
          tags: ghcr.io/${{ github.repository }}-webshare-worker:latest
